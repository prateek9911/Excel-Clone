<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width = device-width, initial-scale =1.0">
        <link rel="stylesheet" href="style.css">
        <title><%=title%></title>
    </head>
    <body>
        <div class="menu">Menu</div>
        <div class="menu-options">Menu Options</div>
        <div class="formulaBar">
            <input type="text" class="text-container">
            <input type="text" class="formula-container">
        </div>
        <div class="cell-containers" > Cells
            
            <%for(let i=1;i<=rows;i++) {%>
                <div class="row">
                    <%for(let j =0;j<cols;j++){%>
                        <div class="col" contenteditable="true">   
                    <%=i + String.fromCharCode(65 + j) %>
                        </div>
                    <%}%>    
                </div>
                <%}%>
        </div>
        
        <div class="sheets"></div>
    </body>
</html>
